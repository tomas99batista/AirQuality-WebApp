application.name=QualityAir Webapp
application.version=1.0-SNAPSHOT

# ===============================
# = DATA SOURCE
# ===============================

# tomas@tomaspc:~$ docker run --name postgres  -d -p 5432:5432 -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=password -d postgres
# tomas@tomaspc:~$ docker exec -it postgres psql -U postgres
# postgres=# SELECT * FROM cities;
# mvn spring-boot:run --debug

spring.datasource.url= jdbc:postgresql://localhost:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=password
#spring.datasource.username=postgres
spring.jpa.hibernate.ddl-auto=create
